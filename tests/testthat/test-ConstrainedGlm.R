context("ConstrainedGlm.R")
test_that("it should create a GLM based on the formula, delta, and data", {
            #set.seed(12345)
  #nobs <- 10000

  #x1 = rnorm(nobs,0,1)
  #x2 = rnorm(nobs,3,1)
  #x3 = rnorm(nobs,10,1)
  #Y = expit(0.3*x1 + 0.1*x2 + 0.8*x3) > 0.5
  #data <- data.table(
    #x1 = x1,
    #x2 = x2,
    #x3 = x3,
    #Y = Y
  #)
  #delta <- 0.35
  #form <- Y ~ x1 + x2 + x3
  #the_glm <- ConstrainedGlm.fit(formula = form, delta = delta, data = data)
})


